/**
 * this component should be displayed only in the url: /about
 */
 
import { FC } from 'react';
 
export const AboutPage: FC = () => {
	return (
		<h1>
			Hello we are currently in the about page
		</h1>
	)
}